import styled from "styled-components";

const Loading = () =>{

    return(
        <Wrapper>
            <h1>Loading...</h1>
        </Wrapper>
    )
}

const Wrapper = styled.div`
    text-align: center;
    width: auto;
`
export default Loading;